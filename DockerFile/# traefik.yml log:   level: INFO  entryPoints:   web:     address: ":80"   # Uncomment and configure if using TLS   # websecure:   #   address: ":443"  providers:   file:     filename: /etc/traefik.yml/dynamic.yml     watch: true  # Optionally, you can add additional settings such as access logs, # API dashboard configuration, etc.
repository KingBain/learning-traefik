# traefik.yml
log:
  level: INFO

entryPoints:
  web:
    address: ":80"
  # Uncomment and configure if using TLS
  # websecure:
  #   address: ":443"

providers:
  file:
    filename: /etc/traefik/dynamic.yml
    watch: true

# Optionally, you can add additional settings such as access logs,
# API dashboard configuration, etc.
